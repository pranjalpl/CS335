package math;
import "fmt";

func main() {

	var prev int_t = 1;
	var curr int_t = 1;
	var next int_t = 1;
	var n int_t = 25;
	var i int_t = 14;
	var j int_t = 12;
	var k int_t = 1;
	var l int_t = 1;
	var one int_t = 1;
	var sum int_t = 0;
	var sum1 int_t = 0;

	// for j = one; j<=n; j = j + one {
	// 	for i=one; i<=n; i = i + one {
			for k=one; k<=n; k = k + one {
				for l=one; l<=n; l = l + one {
					sum = sum + 10;
				};
			};
	// 	};
	// };
	print(j);
	return;
};
